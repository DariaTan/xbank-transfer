# Run parameters for src/training/train_thp.py.
n_clients: null   # cap for a quick debug run; null uses all clients
max_seq_len: 500
max_epochs: 100
patience: 5
valid_frac: 0.05
batch_size: 256
hidden_size: 128
num_layers: 4
lr: 0.001
seed: 0
checkpoint_dir: /app/data/checkpoints/thp
